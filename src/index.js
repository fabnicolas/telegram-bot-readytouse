/*
    NOTICE: use the following require if you want to avoid build/serve process.
    NOT FOR PRODUCTION-USE BECAUSE IT ADDS 1-2 SECONDS MORE TO NODE.JS STARTUP!
*/
// require('babel-core/register');
/*
    For production environment, run Node.js with "npm run build" (If you edited files)
    and "npm run serve" (To execute the server scripts) for best performances.
*/

// Our application starting point.
require('./app.js');